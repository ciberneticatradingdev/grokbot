{
  "name": "grokbot",
  "version": "0.1.0",
  "private": true,
  "description": "grokbot — autonomous AI streamer experiment running on Grok",
  "scripts": {
    "build": "npm --prefix server install --omit=dev",
    "start": "node server/index.js"
  },
  "engines": { "node": ">=20" }
}
